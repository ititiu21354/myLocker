<!-- To run the website, 

download the SQL file, then use Data import to retrieve
database system from the SQL file in the mySQL workbench

in the .env file of the backend folder, change the followings depends on how you set your mySQL server:
Example:
DB_HOST=localhost
DB_USER=Phat
DB_PASSWORD=Phat123

Step 1: type Ctrl + `
Step 2: in the first terminal, type "cd backend", type "node app.js'
Step 3: open new terminal, type "cd frontend", then "npm run start" -->